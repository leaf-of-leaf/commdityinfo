server.port=8083

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url= jdbc:mysql://47.115.1.253:3306/commdityinfo?autoReconnect=true&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=kuangjie

mybatis.mapper-locations=classpath:/mapper/**/*.xml

spring.thymeleaf.cache=false

spring.datasource.hikari.max-lifetime=60000

#actuator
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

#语句打印
#logging.level.com.kj.commdityinfo.mapper=debug


#自定义属性

#origin地址
cors.origin=http://localhost:8080

#一页多少条数据的时候走缓存
page.pageSize=20

#手机验证码过期时间单位，秒
code.sms=60

#是否开启手机验证码功能
code.enableSms=false

#图形验证码过期时间单位，秒
code.validata=60

#分布式锁的对应时间
lock_time=3

#在redis中存放的时间，例如用户在redis中存放的时间，与JWtUtil中对应(一天)
time=86400

#Jwt的过期时间，即在这段时间，对于JWT可以通过验证，不报错(默认一天)
#1000 * 60 * 60 * 24为24小时
jwt.expirition = 86400000

#Jwt的有效时间
#有效期-可用时间=可刷新时间段
#1000 * 60 * 30为30分钟
jwt.time = 1800000